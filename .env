# MongoDB connection string
#
# Local Docker (via docker-compose.yml):
DATABASE_URL="mongodb://localhost:27017/bizztob?replicaSet=rs0&directConnection=true"
#
# MongoDB Atlas example (replace with your own):
# DATABASE_URL="mongodb+srv://<user>:<password>@<cluster>.mongodb.net/bizztob?retryWrites=true&w=majority"

# Change this in production. Any long random string is fine for local dev.
JWT_SECRET="bizztob-dev-secret-change-me-in-production-please-use-a-long-random-string"
