# Postgres connection string
# Matches the credentials in docker-compose.yml.
# If you're using a local (non-Docker) Postgres, change username/password/db to match.
DATABASE_URL="postgresql://bizztob:bizztob@localhost:5432/bizztob?schema=public"

# Change this in production. Any long random string is fine for local dev.
JWT_SECRET="bizztob-dev-secret-change-me-in-production-please-use-a-long-random-string"
